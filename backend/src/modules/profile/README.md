# Profile Module

This folder is reserved for Stackem-owned profile projection code.

Use it when you want:

- Stackem-specific player stats
- a cleaner profile API for this game
- profile reads against the shared `users` collection without editing the old backend repo
